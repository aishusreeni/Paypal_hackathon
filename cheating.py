class save(object):
    def __init__(self,action,target):
        self.action=action
        self.target=target

    def savings()
        if(self.action=="buy")
            if(self.target=="car")
            if(self.target=="xbox")
            if(self.target=="cellphone")
        if(self.action=="study")
        if(self.action=="spend")
            if(self.target=="drink")
            if(self.target=="fruits")